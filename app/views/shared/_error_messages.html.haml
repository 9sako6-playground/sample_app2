- if @user.errors.any?
  %div.bg-red-200.rounded.p-4
    %div.text-left
      The form contains
      = pluralize(@user.errors.count, 'error')
      %ul
      - @user.errors.full_messages.each do |msg|
        %li.text-red-600= msg
